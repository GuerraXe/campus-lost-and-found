package com.campuslostfound.domain;

/** The independent signals the matching engine scores and explains. */
public enum MatchSignal {
    CATEGORY,
    KEYWORDS,
    LOCATION,
    DATE,
    ATTRIBUTES
}
